return require(game:GetService("CoreGui").RobloxGui.Modules.Common.Action)(script.Name, function(v0, v1, v2)
    return {
        appPage = v0, 
        feedItemPosition = v1, 
        numberOfRows = v2
    };
end);
