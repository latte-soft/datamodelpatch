return require(game:GetService("CorePackages").Rodux).makeActionCreator(script.Name, function(v0, v1, v2)
    return {
        tryingOn = v0, 
        assetId = v1, 
        assetTypeId = v2
    };
end);
