return {
    Context = require(script.ProfileQRCodeAnalytics), 
    EventNames = require(script.EventNames), 
    setupFireEvent = require(script.setupFireEvent), 
    useAnalytics = require(script.useAnalytics)
};
