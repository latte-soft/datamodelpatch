return {
    fireDiag = require(script.fireDiag), 
    fireDiagCounter = require(script.fireDiagCounter), 
    fireEventStream = require(script.fireEventStream), 
    createFireEvent = require(script.createFireEvent)
};
