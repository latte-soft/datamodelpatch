return {
    formatRecommendationContextType = require(script.formatRecommendationContextType), 
    formatPresence = require(script.formatPresence), 
    createDiagMetricString = require(script.createDiagMetricString)
};
