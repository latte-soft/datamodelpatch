local v0 = require(script.Array);
local v1 = require(script.AssertionError);
local v2 = require(script.Error);
local v3 = require(script.Map);
local v4 = require(script.Object);
local _ = require(script.Promise);
local v6 = require(script.Set);
local v7 = require(script.Symbol);
local v8 = require(script.Timers);
return {
    Array = v0, 
    AssertionError = v1, 
    Boolean = require(script.Boolean), 
    console = require(script.console), 
    Error = v2, 
    extends = require(script.extends), 
    instanceof = require(script.instanceof), 
    Math = require(script.Math), 
    Number = require(script.Number), 
    Object = v4, 
    Map = v3.Map, 
    coerceToMap = v3.coerceToMap, 
    coerceToTable = v3.coerceToTable, 
    Set = v6, 
    WeakMap = require(script.WeakMap), 
    String = require(script.String), 
    Symbol = v7, 
    setTimeout = v8.setTimeout, 
    clearTimeout = v8.clearTimeout, 
    setInterval = v8.setInterval, 
    clearInterval = v8.clearInterval, 
    util = require(script.util)
};
